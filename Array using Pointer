#include<stdio.h>
main()
{
	int a[5],i,*p,sum=0;
	printf("Enter Elements of array:\n");
	for(i=0;i<5;i++)
	{
		scanf("%d",&a[i]);
	}
	p=a;//p=&a[0];
	for(i=0;i<5;i++)
	{
		sum=sum+*(p+i);
	}
	printf("\Sum of array elements are %d",sum);
}
